name: Genrate ASSCI code

on:
    push
jobs:
    ascii_job:
        runs-on: ubuntu-latest
        steps:
        - name: list file
          run: ls -ltra 
        - name: Executing shell script
          run: ./assci-script.sh
        - name: list file
          run: ls -ltra 